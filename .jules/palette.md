## 2026-05-31 - Found Multiple Icon-Only Buttons Missing ARIA Labels\n**Learning:** This codebase uses a custom `<Button size="icon">` pattern in multiple places (SalesTerminal, MainLayout, ReportsManager) but frequently forgets to include `aria-label`s for these icon-only buttons, reducing accessibility for screen reader users.\n**Action:** Add `aria-label` attributes to icon-only buttons to improve accessibility, starting with `SalesTerminal.tsx` and `MainLayout.tsx`.
