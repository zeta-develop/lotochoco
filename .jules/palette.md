## 2024-05-24 - Add ARIA Labels to Icon-Only Buttons
**Learning:** Found an app-wide pattern where icon-only buttons (like delete, close, and refresh) lack `aria-label`s, breaking screen-reader accessibility for visually impaired users.
**Action:** Always add descriptive `aria-label`s in Spanish to all icon-only buttons to conform to UX accessibility standards.
